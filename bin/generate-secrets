#!/usr/bin/env bash
set -o errexit
set -o pipefail

main() {
  local envname
  envname="$(basename "$(pwd)")"
  envname="${envname//-/_}"
  exec trvs generate-config -f json terraform-config "${envname}"
}

main "$@"
